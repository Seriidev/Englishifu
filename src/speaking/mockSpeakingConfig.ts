/**
 * @deprecated Prefer importing from `../mocks/speakingMock`.
 * Re-export kept so existing `speaking/mockSpeakingConfig` imports keep working.
 */
export {
  speakingMockConfig,
  mockSpeakingConfig,
  TOTAL_SPEAKING_ITEMS,
} from '../mocks/speakingMock'
